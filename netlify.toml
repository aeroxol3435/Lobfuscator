[build]
  command = "cd artifacts/lobfuscator && corepack enable && pnpm install && pnpm build"
  publish = "artifacts/lobfuscator/dist"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
